@page "/site-manager/labor-law-rules"
@layout SiteManagerLayout
@attribute [Authorize(Roles = AppRoles.SiteManagerRule)]
@implements IDisposable

<div class="page-head">
    <div class="ph-t">
        <div class="crumb">پنل مدیر سایت
            <svg class="ic"><use href="assets/img/icons.svg#i-chl"/></svg>
            قوانین کار
        </div>
        <h1>قوانین کار</h1>
        <p>مبالغ قانونی با تاریخ اجرا ثبت می‌شوند؛ فیش‌ها از آخرین قانون فعال استفاده می‌کنند.</p>
    </div>
    <div class="ph-actions">
        <a class="btn btn-p" href="/site-manager/labor-law-rule-form">
            <svg class="ic"><use href="assets/img/icons.svg#i-plus"/></svg>
            قانون جدید
        </a>
    </div>
</div>

<div class="panel">
    <div class="toolbar">
        <select class="in tb-sel" @bind="_selectedKey" @bind:after="OnKeyChanged">
            <option value="">همهٔ کلیدها</option>
            <option value="@Key.MinimumMonthlySalary">حداقل حقوق ماهانه</option>
        </select>
        <span class="chip c-grn ms-auto">
            <span class="c-dot"></span>@((_laborLawRules?.TotalCount ?? 0).ToPersianNumber()) قانون
        </span>
    </div>

    <div class="panel-b p-0">
        @if (_isLoading)
        {
            <div class="text-center p-4">
                <span>در حال بارگذاری...</span>
            </div>
        }
        else if (_laborLawRules is null || _laborLawRules.Items.Count == 0)
        {
            <div class="text-center p-4" style="color:var(--muted)">
                <svg class="ic" style="width:32px;height:32px;opacity:.5">
                    <use href="assets/img/icons.svg#i-doc"/>
                </svg>
                <p class="mt-2">قانونی یافت نشد.</p>
            </div>
        }
        else
        {
            <div class="tblwrap">
                <table class="tbl">
                    <thead>
                    <tr>
                        <th>کلید</th>
                        <th>مقدار (ریال)</th>
                        <th>تاریخ اجرا</th>
                        <th style="text-align:left">عملیات</th>
                    </tr>
                    </thead>
                    <tbody>
                    @foreach (var rule in _laborLawRules.Items)
                    {
                        <tr>
                            <td class="pname">@GetKeyDisplayName(rule.Key)</td>
                            <td class="mnum pos">@(((long)rule.Value).ToPersianNumber())</td>
                            <td class="num">@rule.DisplayEffectiveFrom</td>
                            <td>
                                <span class="rowact">
                                    <a class="icobtn" href="/site-manager/labor-law-rule-form/@rule.Id" title="ویرایش">
                                        <svg class="ic"><use href="assets/img/icons.svg#i-pen"/></svg>
                                    </a>
                                    <button class="icobtn danger" title="حذف"
                                            @onclick="() => ConfirmDelete(rule)"
                                            disabled="@_isDeleting">
                                        <svg class="ic"><use href="assets/img/icons.svg#i-trash"/></svg>
                                    </button>
                                </span>
                            </td>
                        </tr>
                    }
                    </tbody>
                </table>
            </div>
        }
    </div>

    @if (_laborLawRules is not null && _laborLawRules.Items.Count > 0)
    {
        <div class="panel-f">
            نمایش @_laborLawRules.Items.Count.ToPersianNumber() قانون از @_laborLawRules.TotalCount.ToPersianNumber()

            @if (_totalPages > 1)
            {
                <div class="d-flex justify-content-center align-items-center gap-2 ms-auto">
                    <button class="btn btn-g" disabled="@(_pageNumber <= 1)"
                            @onclick="() => GoToPage(_pageNumber - 1)">
                        قبلی
                    </button>

                    @foreach (var pageNum in GetPageNumbers())
                    {
                        if (pageNum == -1)
                        {
                            <span style="padding:0 8px">...</span>
                        }
                        else
                        {
                            <button class="btn @(pageNum == _pageNumber ? "btn-p" : "btn-g")"
                                    @onclick="() => GoToPage(pageNum)">
                                @pageNum
                            </button>
                        }
                    }

                    <button class="btn btn-g" disabled="@(_pageNumber >= _totalPages)"
                            @onclick="() => GoToPage(_pageNumber + 1)">
                        بعدی
                    </button>
                </div>
            }
        </div>
    }
</div>

@if (_ruleToDelete is not null)
{
    <div class="modal-backdrop show" style="z-index:1040"></div>
    <div class="modal show d-block" tabindex="-1" style="z-index:1050">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">تأیید حذف</h5>
                    <button type="button" class="btn-close" @onclick="CancelDelete" disabled="@_isDeleting"></button>
                </div>
                <div class="modal-body">
                    <p>آیا از حذف قانون <b>«@GetKeyDisplayName(_ruleToDelete.Key)»</b> با تاریخ اجرا <b>@_ruleToDelete.DisplayEffectiveFrom</b> اطمینان دارید؟</p>
                    <p class="text-danger" style="font-size:12px">این عملیات قابل بازگشت نیست.</p>
                </div>
                <div class="modal-footer">
                    <button class="btn btn-g" @onclick="CancelDelete" disabled="@_isDeleting">انصراف</button>
                    <button class="btn btn-danger" @onclick="DeleteRule" disabled="@_isDeleting">
                        @if (_isDeleting)
                        {
                            <span>در حال حذف...</span>
                        }
                        else
                        {
                            <span>حذف</span>
                        }
                    </button>
                </div>
            </div>
        </div>
    </div>
}

@code {
    private GetLaborLawRulesResponsePagedResult? _laborLawRules;
    private bool _isLoading;
    private bool _isDeleting;
    private string _selectedKey = string.Empty;
    private int _pageNumber = 1;
    private const int PageSize = 10;
    private int _totalPages => _laborLawRules is null
        ? 0
        : (int)Math.Ceiling(_laborLawRules.TotalCount / (double)PageSize);

    private GetLaborLawRulesResponse? _ruleToDelete;

    protected override async Task OnInitializedAsync()
    {
        await LoadRulesAsync();
    }

    private async Task LoadRulesAsync()
    {
        _isLoading = true;
        StateHasChanged();

        Key? key = string.IsNullOrEmpty(_selectedKey)
            ? null
            : Enum.Parse<Key>(_selectedKey);

        var result = await ApiService.SendAsync(() => ApiClient.GetLaborLawRulesAsync(
            _pageNumber, PageSize, null, key));

        if (result?.IsSuccess is true)
        {
            _laborLawRules = result.Response;
        }

        _isLoading = false;
        StateHasChanged();
    }

    private async Task OnKeyChanged()
    {
        _pageNumber = 1;
        await LoadRulesAsync();
    }

    private async Task GoToPage(int page)
    {
        if (page < 1 || page > _totalPages || page == _pageNumber) return;
        _pageNumber = page;
        await LoadRulesAsync();
    }

    private IEnumerable<int> GetPageNumbers()
    {
        const int visible = 5;
        if (_totalPages <= visible + 2)
        {
            for (var i = 1; i <= _totalPages; i++) yield return i;
            yield break;
        }

        yield return 1;

        var start = Math.Max(2, _pageNumber - visible / 2);
        var end = Math.Min(_totalPages - 1, start + visible - 1);
        start = Math.Max(2, end - visible + 1);

        if (start > 2) yield return -1;
        for (var i = start; i <= end; i++) yield return i;
        if (end < _totalPages - 1) yield return -1;

        yield return _totalPages;
    }

    private void ConfirmDelete(GetLaborLawRulesResponse rule)
    {
        _ruleToDelete = rule;
    }

    private void CancelDelete()
    {
        if (_isDeleting) return;
        _ruleToDelete = null;
    }

    private async Task DeleteRule()
    {
        if (_ruleToDelete is null || _isDeleting) return;

        _isDeleting = true;

        var result = await ApiService.SendAsync(
            apiCall: () => ApiClient.DeleteLaborLawRuleAsync(_ruleToDelete.Id),
            successMessage: "قانون با موفقیت حذف شد."
        );

        _isDeleting = false;

        if (result?.IsSuccess is true)
        {
            _ruleToDelete = null;

            if (_laborLawRules?.Items.Count == 1 && _pageNumber > 1)
            {
                _pageNumber--;
            }

            await LoadRulesAsync();
        }
    }

    private static string GetKeyDisplayName(GetLaborLawRulesResponseKey key) => key switch
    {
        GetLaborLawRulesResponseKey.MinimumMonthlySalary => "حداقل حقوق ماهانه",
        _ => key.ToString()
    };

    public void Dispose()
    {
    }
}
